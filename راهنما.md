# 🤖 راهنمای کامل راه‌اندازی ربات — قدم به قدم

---

## ✅ مرحله ۱ — گرفتن توکن جدید از BotFather

1. تلگرام رو باز کن
2. جستجو کن: `@BotFather`
3. روش کلیک کن و Start بزن
4. بنویس: `/newbot`
5. یه اسم برای ربات بنویس (مثلاً: `ربات گروه من`)
6. یه یوزرنیم بنویس که آخرش `bot` باشه (مثلاً: `mygroupbot`)
7. BotFather یه توکن مثل این میده:
   ```
   1234567890:AAHxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
8. این توکن رو کپی کن — **به کسی نده!**

---

## ✅ مرحله ۲ — گرفتن آیدی عددی خودت

1. توی تلگرام جستجو کن: `@userinfobot`
2. Start بزن
3. آیدی عددیت رو بهت میده (مثلاً: `123456789`)
4. این عدد رو یادداشت کن

---

## ✅ مرحله ۳ — ویرایش فایل کد

فایل `telegram_bot.py` رو باز کن، این دو خط رو پیدا کن:

```python
TOKEN = "YOUR_TOKEN_HERE"
OWNER_IDS = []
```

تغییر بده به:

```python
TOKEN = "توکنت_رو_اینجا_بنویس"
OWNER_IDS = [123456789]    # آیدی عددی خودت
```

---

## ✅ مرحله ۴ — آپلود روی Railway (رایگان، بدون سرور)

### قدم ۴-۱: ساخت حساب Railway
1. برو: **https://railway.app**
2. روی `Login` کلیک کن
3. با **GitHub** لاگین کن (اگه ندارید ثبت‌نام کن — رایگانه)

### قدم ۴-۲: ساخت پروژه
1. بعد از لاگین روی `New Project` کلیک کن
2. `Deploy from GitHub repo` انتخاب کن
3. اگه ریپو نداری: `Deploy a template` → `Python` انتخاب کن

### قدم ۴-۳ (روش ساده‌تر — آپلود مستقیم):
1. روی `New Project` کلیک کن
2. `Empty project` انتخاب کن
3. روی پروژه کلیک کن → `Settings` → `Source`
4. فایل‌هات رو آپلود کن

---

## ✅ مرحله ۵ — آپلود فایل‌ها روی GitHub (روش توصیه شده)

### قدم ۵-۱: ساخت ریپو روی GitHub
1. برو: **https://github.com**
2. ثبت‌نام یا لاگین کن
3. روی `+` کلیک کن → `New repository`
4. اسم بذار: `my-telegram-bot`
5. `Public` انتخاب کن
6. `Create repository` بزن

### قدم ۵-۲: آپلود فایل‌ها
1. روی `uploading an existing file` کلیک کن
2. فایل‌های زیر رو آپلود کن:
   - `telegram_bot.py`
   - `requirements.txt`
   - `Procfile` (ببین مرحله ۶)
3. `Commit changes` بزن

---

## ✅ مرحله ۶ — ساخت فایل Procfile

یه فایل متنی بساز به اسم `Procfile` (بدون پسوند) با این محتوا:

```
worker: python telegram_bot.py
```

این فایل رو هم آپلود کن.

---

## ✅ مرحله ۷ — وصل کردن GitHub به Railway

1. برو Railway → پروژه‌ات
2. `New Service` → `GitHub Repo`
3. ریپوی `my-telegram-bot` رو انتخاب کن
4. Railway خودکار Deploy میکنه!

---

## ✅ مرحله ۸ — اضافه کردن ربات به گروه

1. گروه تلگرامت رو باز کن
2. روی اسم گروه بزن → `Add members`
3. یوزرنیم ربات رو جستجو کن (مثلاً `@mygroupbot`)
4. اضافه‌اش کن

### مهم — دادن دسترسی ادمین به ربات:
1. توی گروه روی اسم ربات کلیک کن
2. `Promote to Admin` بزن
3. این دسترسی‌ها رو فعال کن:
   - ✅ Delete messages
   - ✅ Ban users
   - ✅ Add new admins
   - ✅ Pin messages
   - ✅ Invite via link
4. `Save` بزن

---

## ✅ مرحله ۹ — تست ربات

توی گروه بنویس:
- `/start` — شروع
- `/help` — راهنما
- `/panel` — پنل مدیریت
- `/ping` — چک کردن ربات

---

## 📱 اگه از گوشی استفاده می‌کنی:

### روش ۱ — GitHub Mobile:
- اپ GitHub رو نصب کن
- فایل‌ها رو آپلود کن (از مرحله ۵-۲)

### روش ۲ — GitHub Desktop از مرورگر:
- همه مراحل رو از مرورگر گوشی انجام بده
- فقط github.com و railway.app رو باز کن

---

## ❓ سوالات رایج:

**سوال: ربات پیام نمیده؟**
→ چک کن ادمینش کردی؟ توکن درسته؟

**سوال: خطای `Forbidden` میده؟**
→ ربات رو ادمین کن با دسترسی‌های بالا

**سوال: Railway چقدر رایگانه؟**
→ ماهی ۵ دلار اعتبار رایگان میده — برای یه ربات کافیه

**سوال: داده‌های ربات کجا ذخیره میشه؟**
→ فایل `bot_data.json` در کنار کد

---

## 🎯 دستورات مهم ربات:

| دستور | کار |
|-------|-----|
| `/panel` | پنل مدیریت با دکمه |
| `/ban` | بن کردن (ریپلای بزن) |
| `/unban` | رفع بن (ریپلای بزن) |
| `/promote` | ادمین کردن (ریپلای بزن) |
| `/demote` | حذف از ادمینی (ریپلای بزن) |
| `/mute` | بی‌صدا کردن |
| `/unmute` | رفع بی‌صدا |
| `/warn` | اخطار (۳ اخطار = بن) |
| `/del` | حذف پیام (ریپلای بزن) |
| `/tag` | تگ همه |
| `/tagadmin` | تگ ادمین‌ها |
| `/setwelcome` | تنظیم خوشامد (با عکس/گیف) |
| `/save` | سیو پیام |
| `/addfilter` | فیلتر کلمه |
| `/rules` | قوانین |
